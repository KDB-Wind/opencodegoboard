{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the Tauri shell prototype",
  "windows": ["main"],
  "permissions": ["core:default"]
}
